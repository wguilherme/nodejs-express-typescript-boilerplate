import auth from './routes/auth'
import item from './routes/item'
import user from './routes/user'
import general from './routes/general'

const routes = [auth, item, user, general]

export default routes
